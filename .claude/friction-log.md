# Friction log

> Tier-1 scratch for the capability advisor (see CLAUDE.md). One line per **real,
> behavior-changing** friction noticed at task wrap-up — workflow, the owner's
> prompting, code structure, or token waste. Most tasks add nothing; silence is
> correct. When the same line recurs ~2–3×, I suggest promoting it to a `feedback`
> memory or a CLAUDE.md rule — that promotion waits for the owner's green light.

Format: `YYYY-MM-DD · <category> · <one line> [×N]`
Categories: `workflow` · `prompt` · `code` · `tokens`

---

- 2026-06-19 · workflow · Screenshot tool times out on continuous-WebGL pages (hero orb / mesh-drift) — verify those structurally (DOM / preview_eval); screenshot only non-WebGL pages.
- 2026-06-19 · workflow · Turbopack serves stale CSS/routes after edits — stop dev server, `rm -rf .next`, restart.
- 2026-06-19 · workflow · Media-query-gated UI (`prefers-reduced-motion` / `pointer: coarse`) can't be emulated via preview tools — verify the default path + the conditional's logic, hand the gated branch to the owner's OS toggle / a real phone.
- 2026-06-19 · workflow · `preview_screenshot` scales wide/desktop split-screens down too far to judge proportions — confirm layout with `getBoundingClientRect` / computed `gridTemplateColumns`, not by eyeballing the image.
- 2026-06-19 · workflow · Auth/signup failures show only the app's generic masked error — debug via Supabase MCP `get_logs(auth)` to see the real GoTrue cause (this one: SMTP `550` "Error sending confirmation email", i.e. a dashboard SMTP config issue, not a code bug).
- 2026-06-19 · workflow · This repo's Codex reviewer auto-reviews on PR open — wait for its pass before declaring "ready to merge". It caught a P1 (auth account-takeover) my own multi-agent pre-merge review missed.
- 2026-06-19 · code · Auth-callback review heuristic: `verifyOtp`/`exchangeCodeForSession` mint a session for ANY OTP type, so a caller-controlled redirect param can steer a signup link into the password-reset flow → takeover. Check session-minting + redirect-target together; open-redirect-to-`next` is not the only risk.
- 2026-06-21 · code · SSR-safe client-only flags in marketing go through `useSyncExternalStore` (server snapshot `false`), like the `useMediaQuery` helper in smooth-scroll-provider — the naive `useEffect`+`setState` trips the `react-hooks/set-state-in-effect` lint rule. Used it for the new WebGL-availability probe.
- 2026-06-21 · code · Any R3F `<Canvas>` mount must be gated on a WebGL feature-probe AND wrapped in an error boundary — Three.js throws "Error creating WebGL context" (uncaught → Sentry) when the GPU process is unavailable (hardware accel off / blocklisted). reduced-motion + coarse-pointer guards don't cover this.
- 2026-06-19 · workflow · Owner correction: on a reviewer-flagged security bug, fix the CLASS, not the flagged line — grep for parallel/duplicate handlers (a CLIENT twin re-introduced the same auth-intent flaw the server fix missed) and audit the privileged sink BEFORE pushing. Reactive one-line patches just feed the review loop (took 3 rounds before a full audit). [promotion candidate → CLAUDE.md rule] **[×2, 2026-06-24 — non-security recurrence]** Codex flagged un-timed-out `fetch`es one at a time across 4 separate rounds (send adapter → worker→app webhook → /pair → /status); sweeping EVERY bridge fetch when adding the first timeout would have collapsed 4 review rounds into 1. The class-sweep rule is provider/domain-agnostic — when a reviewer flags one instance of a pattern, grep the whole codebase for siblings and fix them in the same push. [STRONG promotion candidate → CLAUDE.md rule]
- 2026-06-23 · workflow · To QA auth-gated client UI (onboarding, etc.) without logging in, drop a throwaway `src/app/<name>/page.tsx` harness that renders the component with mock initialState (+ a temporary `previewMode` prop to advance steps locally past the auth-gated server action), screenshot via Playwright→localhost (file:// is blocked), then STRIP the route + previewMode before commit. Two gotchas: a `__`-prefixed route folder is a private (non-routable → 404) Next folder; and after deleting a route you must `rm -rf .next` or the build fails on stale generated `.next/types` referencing the gone page.
- 2026-06-23 · workflow · Verifying JS→DB aggregation refactors WITHOUT signed-in browser QA: use the Supabase MCP `execute_sql` (read-only) to cross-check the new query's output against the OLD JS logic's equivalent SQL on real prod data, alongside fixture unit tests. This caught a real bug: `startAt` is `timestamp without time zone` (Prisma stores UTC there), so `startAt AT TIME ZONE <tz>` mis-buckets instants near midnight — the correct zoned-day expression is `(startAt AT TIME ZONE 'UTC') AT TIME ZONE <tz>`. Always check the column's data_type before writing AT TIME ZONE.
- 2026-06-23 · workflow · The auto-mode classifier blocks Supabase `apply_migration` (prod DDL) even under a broad "do everything / optimize" mandate — it wants per-action approval for shared prod-state writes. Don't fight it: keep the schema as source of truth, ship a ready-to-run `prisma/perf-indexes.sql`, and hand the apply to the owner (db push). Plan around it rather than re-attempting the blocked call.
- 2026-06-23 · workflow · Audit-and-fix loop: land the verification baseline (tests) FIRST, not mid-loop. I added Vitest in iteration 2; the parity-critical fixes (atomic appointment save, payment ledger, reports aggregation) all wanted characterization tests to exist before touching them. On a no-test-suite repo, "establish a test baseline" is genuinely finding #1 — do it before the risky fixes, not after. Also: the owner's "4/10" self-estimate was pessimistic — the audit found tenant isolation/auth were uniformly correct (no IDOR/injection); the real debt was scaling cliffs (unbounded queries, JS aggregation) + no tests + a few money/tz bugs. Audit before believing a severity estimate.
- 2026-06-23 · workflow · Fan-out review agents recommend fixes without project history — one flagged "claim-before-send" for reminder idempotency as [FIX NOW], but PROJECT_STATUS shows Codex already rejected exactly that (it DROPS a reminder on crash) and the team reverted to send-then-record. Before acting on an agent's recommended fix, grep PROJECT_STATUS / git log for whether it was already tried-and-reverted; otherwise you re-litigate a settled decision. (Same theme as [[recovery-marker-hardening]]: don't silently re-fix differently.) [×2 → promotion candidate]
- 2026-06-23 · workflow · Vercel **Hobby** plan caps cron jobs at **once per day** — any sub-daily `vercel.json` schedule (e.g. `0 * * * *`) fails the deploy outright ("Hobby accounts are limited to daily cron jobs"). Before proposing a more-frequent cron as a fix, check the plan; sub-daily reminders need Vercel Pro or an external scheduler (GitHub Actions / cron-job.org) hitting the route with `CRON_SECRET`. (Caught the 2-hour-reminder fix at deploy, not before.)
- 2026-06-24 · workflow · Booting the WhatsApp worker via background `npm run start` leaves the tsx/node CHILD alive after killing the npm wrapper pid — and because the worker imports the root `@prisma/client`, that child keeps `node_modules/.prisma/client/query_engine-windows.dll.node` open, so the next `prisma generate`/`npm run build` fails with `EPERM: rename …query_engine-windows.dll.node.tmp`. Kill the actual node child (Get-Process node → match StartTime), not just the npm pid; the engine DLL is the tell. Run the worker with `npm run start` directly (not via the npm wrapper) when you need a clean kill.
- 2026-06-23 · code · Ambient animated backgrounds (parallax orbs) perf checklist: cache `innerWidth/innerHeight` outside the `pointermove` handler (per-event layout read otherwise), gate the listener on coarse pointer + reduced-motion, and don't stack `backdrop-blur` cards under big `blur-3xl` orbs (the backdrop re-sample is the real per-frame cost). Put the CSS drift animation and the framer `x/y` parallax on DIFFERENT elements (wrapper vs inner) so the transforms don't fight.
- 2026-06-28 · code · `revalidatePath` completeness has two blind spots a per-feature sweep misses: (1) **shell-global identity** (owner name/phone, business name/logo/accent) is server-fetched in `(workspace)/layout.tsx` and renders on EVERY route → needs `revalidatePath("/", "layout")`, never a page-list; (2) **mutations outside the obvious feature folders** — `(auth)/updateOwnerProfileAction` had ZERO revalidate (genuinely stale, not just masked by the fork's blanket-refresh), and staff/settings revalidated partial path-lists (staff also revalidated dead `/settings`). When auditing stale-after-mutation, grep ALL `actions.ts` incl. `(auth)` for mutation-without-matching-revalidate, and treat layout-global data as its own class. 3rd recurrence of the incomplete-coverage theme → [[deep-review-before-push]] / [[perf-pass-revalidation-and-pool]]; promotion candidate → CLAUDE.md revalidation rule.
- 2026-06-24 · workflow · **Cost a deleted gitignored `.env`.** `cd DIR && cmd > log 2>&1 &` backgrounds the WHOLE pipeline in a SUBSHELL, so the `cd` never moves the MAIN shell. A later same-script `rm -f .env` (intended for DIR) then runs at the ORIGINAL cwd → deleted the repo-root `.env` (gitignored → unrecoverable from git). Rules: for a backgrounded job use an explicit subshell `( cd DIR && cmd ) &`; do cleanup with ABSOLUTE paths, never a bare `rm` that assumes a prior `&`-cd took effect; and never `rm` a `.env`-style secrets file in a script at all — `mv` to a temp name or scope it to a path you just created.
- 2026-06-29 · workflow · **"Invisible until JS" first-paint symptoms have an ANCESTOR cause, not just the named element.** The landing "Start free shows, then the page 1–2s later" wasn't (only) the hero: marketing `PageTransition` wrapped the ENTIRE page body in a framer-motion `motion.div initial={{opacity:0}}` baked into the SSR HTML, so everything stayed invisible until hydration — and CSS-fixing only the hero did nothing because its opacity:0 ancestor still hid it. When chasing an SSR opacity/transform gate, trace the FULL ancestor chain (grep page-transition / shell wrappers), not just the element in view. Fix pattern: CSS-driven entrance (`animation … both`) runs at paint with no JS dependency; a keyed remount replays it on nav. Verify by checking for an `opacity:0` ancestor of the content in the raw SSR HTML, not just the leaf.
- 2026-06-29 · code · **Supabase `getClaims()` can't replace `getUser()` for the email-confirmation gate.** The ES256 access-token JWT carries `sub/email/user_metadata/app_metadata/role` but NOT `email_confirmed_at` — which the middleware + onboarding pages gate on. `getClaims` is local (no network) ONLY with asymmetric keys (this project IS ES256 — verified via `/auth/v1/.well-known/jwks.json`). The safe scoped win is the workspace-context path (`requireCurrentWorkspace`/`toBusinessIdentity` use only id/email/user_metadata, never `email_confirmed_at`) → a claims fast-path there cuts one of the two per-nav `getUser` RTTs. But shipping it needs signed-in QA, which the password-entry safety rule blocks me from doing autonomously — so it's owner-gated. Ties to [[perf-pass-revalidation-and-pool]] (which deferred this) and [[signed-in-test-account]].
- 2026-06-29 · workflow · A **static-screenshot scoring gate plateaus** on motion-heavy / depth-driven design work. Driving a harsh 4-critic panel toward a numeric bar (9.2) on JPEGs hit a hard ceiling ~9.1 (avg jittered 8.68→9.15→9.05→9.13 with ±0.25 panel noise) for two structural reasons: (1) stills can't show the animation that IS the deliverable (count-ups, chart-draw, typing, crossfade, sticky-scroll, tilt) — rubric "is it alive?" is unscorable from a frame; (2) a white pane on a white section ALWAYS reads flatter than dark-on-dark in a JPEG regardless of shadow, so "unify pane depth" recurs forever even after applying the exact recipe (2 critics call it resolved, 2 don't — perceptual, not fixable). Lessons: budget ~300k tokens PER score round (5 rounds ≈ 1.5M) — score 2–3× max, not until-it-clears; treat the convergent defect LIST as signal and the average as noise; tell critics motion is verified-in-browser so they don't dock invisible-in-stills items; and recognize the ceiling rather than ping-pong contradictory asks (round 3 "remove ghost tracks" vs round 5 "add ghost tracks"; round 4 chart "too flat" → I fixed it → round 5 scored a STALE image of the old chart). For motion work, capture a GIF or score live, or accept the owner is the final arbiter once the static gate plateaus. [promotion candidate if it recurs]
- 2026-06-29 · workflow · **Anchor a harsh-critic scoring panel to REAL competitor screenshots, not a self-defined rubric** — it breaks the self-referential plateau (see the static-gate line above). Captured stripe.com live via Playwright (navigate + scroll + screenshot) and fed those JPEGs to the panel beside Vela's. Against a real bar the panel scored a brutal 7.6 (vs the ~9.1 ceiling a self-rubric gave the *prior* version) and — critically — produced CONVERGENT, concrete, buildable defects: all 3 critics independently named "gradient charts / flat shadows / flat single-temp stages", then next round "unlit dark stages / flat hero KPIs". Iterating that convergent list moved the score monotonically (7.6 → 8.73 → 8.83) instead of jittering ±0.25. Lessons: real-competitor capture is the cheap unlock; treat 3-way convergence as the true majors (act on those, ignore lone-critic taste); and the gains decelerate fast (+1.13, +0.10) so 2-3 rounds is the budget — past that the owner is the right final judge (the static gate can't see verified live motion). [reinforces the static-screenshot-gate line]
- 2026-06-30 · workflow · **Automating a "merge if Codex is clean" gate has three sharp edges — one caused a real premature merge (shipped 3 unaddressed P2s).** (1) The bot login differs by API surface: `gh pr view --json reviews` returns `chatgpt-codex-connector`, but the REST endpoints (`/pulls/{n}/reviews`, `/comments`, `/issues/{n}/reactions`) return `chatgpt-codex-connector[bot]`. An EXACT-string filter against REST silently returns `[]` → I merged on a false "clean". Match as a substring (`test("(?i)codex")`). (2) Codex's 👀 (`eyes`) reaction = "reviewing now", NOT a verdict — only 👍`+1`/👎`-1` is; don't break the poll on any reaction. (3) Codex re-reviews only on PR-open / ready / an `@codex review` comment (not every push), and it reviews a specific commit — filter findings by `commit_id` startswith HEAD and assert "0 findings on HEAD" explicitly before merging; never infer clean from an empty poll.
- 2026-06-30 · code · **A JS `useMediaQuery` gate can't fix an SSR/first-paint layout problem.** `useSyncExternalStore` returns the server snapshot (`false`) on the server AND the first client render, so a width-gated branch renders the wrong layout (e.g. a text-only tour) until hydration flips it — and never at all with JS off. Codex flagged exactly this on a "render the below-lg pane" fix. The pre-hydration-correct tool is CSS: render the alternate content always and toggle with `lg:hidden` / `hidden lg:block` so it's in the SSR HTML and revealed at paint. Verify by curling the raw SSR HTML for the content markers, not by eyeballing the hydrated page.
- 2026-06-29 · workflow · **The owner's gestalt "5/10" beat my critic panel's "8.9" — because I optimized the WRONG axis.** I chased data-DENSITY (crammed full app dashboards into small panes at ~8px text) and the panel rewarded it; the owner twice said "still 5/10, look at stripe.com." Going back to Stripe's actual pages revealed the real gap: Stripe shows ONE large, FOCUSED, LEGIBLE product surface at comfortable reading size (14–16px) as the section hero — the opposite of a busy miniature. Rebuilding every pane at a readable scale (≈1.5× type, bigger tiles, fewer competing elements, pane made dominant in the layout) is what a fresh panel called "decisively closed the cramped-miniature gap." Lessons: (1) when the owner's holistic score and your metric diverge by >2 points, your METRIC is measuring the wrong thing — re-derive the rubric from the reference, don't defend the number; (2) for product-mockup marketing, LEGIBILITY/SCALE dominates density — a screenshot panel happily over-rewards cramming because it can't feel "I can't read this"; (3) a 1fr CSS-grid column won't shrink below a `truncate` child's max-content, so one nowrap line in a sibling column silently starves the hero column (fix: `minmax(0,1fr)` + `flex-1` on the truncating wrapper + let copy wrap). Ties to [[static-screenshot-gate]] and the competitor-anchoring line above.
- 2026-06-29 · workflow · Adversarial review of a **fallback-hook** (e.g. `useWorkspaceUnreadCount(serverValue)`) must trace the PROVIDER'S SEED/initial value, not just confirm "provider present → reads the live value". Codex caught a P2 two of my pre-push agents missed: the dashboard Unread-messages KPI flashed a stale **0** on load because the app shell seeds `WorkspaceLiveProvider` with `unreadCount=0`, so the hook returned the context value over the server fallback until the first poll resolved. Fix pattern: gate the live source behind an `initialized` flag so consumers keep their server-rendered value until real data actually arrives (also hardens hidden-tab / failed-poll). When verifying "reads the live value", always follow the context's INITIAL value through to first render. Ties to [[deep-review-before-push]] — the recurring miss is reviewing steady-state but not initial-state.
